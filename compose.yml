version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: server
    volumes:
      - /usr/src/node_modules
      - .:/usr/src/app
    command: yarn dev
    expose:
      - 3000
    ports:
      - '3000:3000'
